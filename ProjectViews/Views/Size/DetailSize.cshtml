@model Data.Models.Sizes

@{
    ViewData["Title"] = "DetailSize";
}

<h1>DetailSize</h1>

<div>
    <h4>Sizes</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.SizeNumber)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.SizeNumber)
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "UpdateSize", new { id = Model.Id})|
    <a asp-action="ShowSize">Back to List</a>
</div>
